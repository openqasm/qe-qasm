OPENQASM 3.0;

// This is a test of UTF8 character encoding in
// OpenQASM3 programs. It has no programmatic
// meaning.

// uint count = 15;
uint räkna = 15;

// int one = 1;
int ett = 1;

// int two = 2;
int två = 2;

// int three = 3;
int tre = 3;

// int four = 4;
int fyra = 4;

// int five = 5;
int fem = 5;

// gate resilient(lambda, theta, phi) bear {
//  U(phi, theta, lambda) bear;
// }
gate uthållig(λ, φ, θ) björn {
  U(θ, φ, λ) björn;
}

// gate ferocious(lambda, theta, phi) fox {
//  U(phi, theta, lambda) fox;
// }
gate våldsam(λ, φ, θ) räv {
  U(θ, φ, λ) räv;
}
